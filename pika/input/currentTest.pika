exec {	
	var n1 := [[1, 3], [5, 7], [9, 11]].
	var n2 := [[1, 2], [3, 5], [7, 11]].
	
	const makePair := <int x, int y> -> [int] {
		return [x, y].
	}.
	
	const makeArrPair := <[int] x, [int] y> -> [[int]] {
		return [x, y].
	}.
	
	var pairList := zip n1, n2, makeArrPair.
	print pairList.
	
	n1[0][0] := 99.
	
	print _n_, pairList.
}